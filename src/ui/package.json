{
  "name": "@miniopencode/ui",
  "version": "1.0.0",
  "description": "Cross-platform desktop UI for MiniOpenCode",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc && tsc -p tsconfig.main.json",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^33.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}