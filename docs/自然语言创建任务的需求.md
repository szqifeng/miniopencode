1. 任务名称保持不变，需要基于 AI 自动解析用户的任务描述，页可以支持用户自己修改
2. 输入文件路径修改为，文件上传，文件上传到一个任务纬度的文件夹，并且 agent 的运行的工作空间都设置为对应的工作目录。
3. 执行方式改为手动执行，每日执行，每周，每小时，时间说明修改一下 改为联动的 每周就是可以选择周几的具体时间，每日也是 每小时也是
4. 用户的任务描述，在聊天窗口中的交互，在保存任务的时候，都需要抽象为一个具体的分析目标文本。